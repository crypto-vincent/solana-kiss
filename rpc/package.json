{
  "name": "solana-kiss-rpc",
  "version": "0.0.6",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "prettier": "^3.5.3",
    "prettier-plugin-organize-imports": "^4.3.0",
    "ts-jest": "^29.3.4",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "solana-kiss-data": "0.0.6"
  },
  "scripts": {
    "build": "tsup src/index.ts --dts --clean --minify --sourcemap --out-dir dist",
    "fmt": "prettier --write src && prettier --write tests",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testTimeout": 10000,
    "testMatch": [
      "**/tests/*.ts"
    ]
  }
}
